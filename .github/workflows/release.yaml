name: 🚀 Deploy KOKKOK WEB service to S3

on:
  workflow_dispatch:
    inputs:
      SERVICE_NAME: # NOTE: 폴더 이름과 동일하게 설정 필요
        description: "배포 서비스 이름"
        required: true
        type: choice
        options: [carInspection, moveRanking]
      DEPLOY_ENV:
        description: "배포 환경"
        required: true
        type: choice
        options: [개발]
      VERSION:
        description: "배포할 버전 (예: v1.0.0 or v1.0.0-n)"
        required: true
        type: string

run-name: "🚀 ${{ inputs.SERVICE_NAME }} / ${{ inputs.VERSION }} 버전 / ${{ inputs.DEPLOY_ENV }} 환경 CD 실행"

env:
  # NOTE: Workflow 공통 설정
  SNAPSHOT_PREFIX: "__snapshot"
  VERSION_FILE_PREFIX: "__current_release"
  BUILD_FOLDER: ""
  SNAPSHOT_FOLDER: ""

jobs:
  # NOTE: 1) validation.yaml 호출 - AWS Config 검증, 유효성 검증, AWS 인증 및 실존 리소스 검증
  validate-cd:
    uses: ./.github/workflows/validation.yaml
    with:
      SERVICE_NAME: ${{ inputs.SERVICE_NAME }}
      DEPLOY_ENV: ${{ inputs.DEPLOY_ENV }}
      VERSION: ${{ inputs.VERSION }}
    secrets:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  # NOTE: 2) S3 직접 배포
  deploy-to-aws:
    runs-on: ubuntu-latest
    needs: [validate-cd]
    steps:
      - name: "🪪 AWS IAM KEY 설정"
        run: |
          SERVICE_NAME="${{ inputs.SERVICE_NAME }}"
          KEY_LABEL=""

          case "$SERVICE_NAME" in
            moveRanking)
              echo "AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }}" >> $GITHUB_ENV
              echo "AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }}" >> $GITHUB_ENV
              KEY_LABEL="MOVE RANKING"
              ;;
            carInspection)
              echo "AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }}" >> $GITHUB_ENV
              echo "AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }}" >> $GITHUB_ENV
              KEY_LABEL="CAR INSPECTION"
              ;;
            *)
              printf "::error:: ⚠️ %s 지원하지 않는 서비스입니다.\n" "$SERVICE_NAME"
              exit 1
              ;;
          esac

          printf "::notice:: ✅ %s AWS IAM KEY 설정 완료\n" "$KEY_LABEL"

      - name: "🔍 배포할 빌드 폴더 존재 여부 확인"
        run: |
          S3_BUCKET="s3://${{ needs.validate-cd.outputs.S3_BUCKET_NAME }}"
          S3_REGION="${{ needs.validate-cd.outputs.S3_DEPLOY_BUCKET_REGION }}"
          # NOTE: 빌드 폴더 이름 형식: {환경}-{서비스명}-{버전}
          ENV_SUFFIX="$([[ "${{ inputs.DEPLOY_ENV }}" == "개발" ]] && echo "dev" || echo "prod")"
          BUILD_FOLDER="__${ENV_SUFFIX}-${{ inputs.SERVICE_NAME }}-${{ inputs.VERSION }}"

          printf "::notice:: 🔍 S3 버킷에서 %s 폴더 존재 여부 확인 중\n" "$BUILD_FOLDER"

          # NOTE: 빌드 폴더 존재 여부 확인
          if ! aws s3 --region "$S3_REGION" ls "${S3_BUCKET}/${BUILD_FOLDER}/" | grep -q .; then
            printf "::error:: 🚨 S3 버킷에 %s 폴더가 존재하지 않습니다.\n" "$BUILD_FOLDER"
            printf "::error:: 🚨 먼저 CI 워크플로우를 실행하여 빌드를 생성해주세요.\n"
            exit 1
          fi

          printf "::notice:: ✅ S3 버킷에 %s 폴더가 존재합니다.\n" "$BUILD_FOLDER"

          # NOTE: 다음 스텝에서 사용하기 위해 환경변수로 저장
          echo "BUILD_FOLDER=$BUILD_FOLDER" >> $GITHUB_ENV

      - name: "📸 현재 배포 버전 스냅샷 생성"
        run: |
          S3_BUCKET="s3://${{ needs.validate-cd.outputs.S3_BUCKET_NAME }}"
          S3_REGION="${{ needs.validate-cd.outputs.S3_DEPLOY_BUCKET_REGION }}"

          # NOTE: S3 루트에서 현재 배포된 버전 파일 찾기
          CURRENT_VERSION_FILE=$(aws s3 ls "$S3_BUCKET/" --region "$S3_REGION" | awk '{print $4}' | grep "^${{ env.VERSION_FILE_PREFIX }}-" | head -n 1)

          if [ -n "$CURRENT_VERSION_FILE" ]; then
            # NOTE: 파일명에서 __current_release- 이후 부분 추출하고 .txt 제거
            VERSION_SUFFIX="${CURRENT_VERSION_FILE#${{ env.VERSION_FILE_PREFIX }}-}"
            VERSION_SUFFIX="${VERSION_SUFFIX%.txt}"
            SNAPSHOT_FOLDER="${{ env.SNAPSHOT_PREFIX }}-${VERSION_SUFFIX}"
            printf "::notice:: 📸 현재 배포된 버전: %s\n" "$VERSION_SUFFIX"
          else
            # NOTE: 버전 파일이 없으면 기본 스냅샷 폴더 사용 (최초 배포)
            SNAPSHOT_FOLDER="${{ env.SNAPSHOT_PREFIX }}"
            printf "::notice:: 📸 현재 배포된 버전 파일이 없습니다 (최초 배포)\n"
          fi

          printf "::notice:: 📸 현재 루트 파일들을 %s 폴더로 스냅샷 생성 중\n" "$SNAPSHOT_FOLDER"

          # NOTE: 기존 스냅샷 폴더가 있다면 삭제
          if aws s3 --region "$S3_REGION" ls "${S3_BUCKET}/${SNAPSHOT_FOLDER}/" 2>/dev/null | grep -q .; then
            printf "::notice:: 🗑️ 기존 스냅샷 폴더 삭제 중...\n"
            aws s3 rm --recursive --region "$S3_REGION" "${S3_BUCKET}/${SNAPSHOT_FOLDER}/"
          fi

          # NOTE: S3 루트의 파일들을 스냅샷 폴더로 복사 (버저닝 폴더와 현재 스냅샷 폴더만 제외)
          aws s3 cp --recursive --region "$S3_REGION" "$S3_BUCKET" "${S3_BUCKET}/${SNAPSHOT_FOLDER}" \
            --exclude "__*/*" \
            --exclude "${SNAPSHOT_FOLDER}/*" || {
            printf "::warning:: ⚠️ 스냅샷 생성 중 일부 파일이 없을 수 있습니다. (최초 배포일 수 있음)\n"
          }

          printf "::notice:: ✅ 스냅샷 생성 완료: %s\n" "$SNAPSHOT_FOLDER"

          # NOTE: 다음 스텝에서 사용하기 위해 환경변수로 저장
          echo "SNAPSHOT_FOLDER=$SNAPSHOT_FOLDER" >> $GITHUB_ENV

      - name: "🔬 새 버전 파일 무결성 검증 (Atomic 배포 전략)"
        run: |
          S3_BUCKET="s3://${{ needs.validate-cd.outputs.S3_BUCKET_NAME }}"
          S3_REGION="${{ needs.validate-cd.outputs.S3_DEPLOY_BUCKET_REGION }}"
          BUILD_FOLDER="${{ env.BUILD_FOLDER }}"

          printf "::notice:: 🔬 S3 루트 정리 전 새 버전 파일 무결성 검증 시작\n"
          printf "::notice:: 📁 검증 대상: %s\n" "$BUILD_FOLDER"

          # NOTE: 1) 필수 진입점 파일(index.html) 존재 여부 확인
          if ! aws s3 ls "${S3_BUCKET}/${BUILD_FOLDER}/index.html" --region "$S3_REGION" | grep -q "index.html"; then
            printf "::error:: 🚨 치명적 오류: index.html 파일이 존재하지 않습니다\n"
            printf "::error:: 🚨 경로: %s/index.html\n" "$BUILD_FOLDER"
            printf "::error:: 🚨 S3 루트 정리를 중단합니다 (안전한 상태 유지)\n"
            exit 1
          fi
          printf "::notice:: ✅ index.html 존재 확인\n"

          # NOTE: 2) assets 폴더 존재 여부 확인 (빌드 산출물 필수 구조)
          if ! aws s3 ls "${S3_BUCKET}/${BUILD_FOLDER}/assets/" --region "$S3_REGION" | grep -q .; then
            printf "::warning:: ⚠️ assets 폴더가 비어있거나 존재하지 않습니다\n"
            printf "::warning:: 📁 경로: %s/assets/\n" "$BUILD_FOLDER"
            # NOTE: assets가 없어도 배포는 진행 (경고만 표시)
          else
            printf "::notice:: ✅ assets 폴더 존재 확인\n"
          fi

          # NOTE: 3) 빌드 폴더의 총 크기 확인 (최소 크기 검증)
          FOLDER_SIZE=$(aws s3 ls "${S3_BUCKET}/${BUILD_FOLDER}/" --region "$S3_REGION" --recursive --summarize | grep "Total Size" | awk '{print $3}')

          if [ -z "$FOLDER_SIZE" ] || [ "$FOLDER_SIZE" -eq 0 ]; then
            printf "::error:: 🚨 치명적 오류: 빌드 폴더가 비어있습니다\n"
            printf "::error:: 🚨 경로: %s\n" "$BUILD_FOLDER"
            printf "::error:: 🚨 S3 루트 정리를 중단합니다 (안전한 상태 유지)\n"
            exit 1
          fi

          # NOTE: 크기를 MB 단위로 변환하여 표시
          FOLDER_SIZE_MB=$(echo "$FOLDER_SIZE" | awk '{printf "%.2f", $1/1024/1024}')
          printf "::notice:: ✅ 빌드 폴더 크기: %.2f MB (%s bytes)\n" "$FOLDER_SIZE_MB" "$FOLDER_SIZE"

          # NOTE: 4) 최소 크기 임계값 검증 (100KB 이상)
          MIN_SIZE=$((100 * 1024)) # 100KB
          if [ "$FOLDER_SIZE" -lt "$MIN_SIZE" ]; then
            printf "::error:: 🚨 치명적 오류: 빌드 폴더 크기가 비정상적으로 작습니다\n"
            printf "::error:: 🚨 현재 크기: %.2f MB (최소 요구: 0.10 MB)\n" "$FOLDER_SIZE_MB"
            printf "::error:: 🚨 S3 루트 정리를 중단합니다 (안전한 상태 유지)\n"
            exit 1
          fi

          printf "::notice:: ✅ 새 버전 파일 무결성 검증 완료\n"
          printf "::notice:: ✅ S3 루트 정리를 안전하게 진행할 수 있습니다\n"

      - name: "🗑️ S3 루트 완전 정리 (버저닝 폴더와 스냅샷만 유지)"
        run: |
          S3_BUCKET="s3://${{ needs.validate-cd.outputs.S3_BUCKET_NAME }}"
          S3_REGION="${{ needs.validate-cd.outputs.S3_DEPLOY_BUCKET_REGION }}"

          printf "::notice:: 🗑️ S3 루트 완전 정리 시작\n"

          # NOTE: 삭제 실패 추적을 위한 카운터 초기화
          FAILED_DELETIONS=0
          TOTAL_DELETIONS=0
          KEPT_FILES=0

          # NOTE: S3 루트의 모든 객체 목록 가져오기 (파일과 폴더)
          aws s3 ls "$S3_BUCKET/" --region "$S3_REGION" --recursive | awk '{print $4}' | while read -r object; do
            if [ -n "$object" ]; then
              # NOTE: 보호 대상 폴더 패턴 (새 배포 플로우와 이전 배포 플로우 모두 유지)
              # NOTE: - __*/* : 새 배포 플로우의 버저닝 폴더 (예: __dev-moveAdmin-v1.0.0/, __snapshot-...)
              # NOTE: - build/* : 이전 배포 플로우에서 배포된 빌드 폴더
              # NOTE: - build-before* : 이전 배포 플로우에서 직전 빌드 폴더 (build-before/, build-before-*/ 포함)
              # NOTE: - build-legacy* : 이전 배포 플로우에서 레거시 빌드 폴더 (build-legacy/, build-legacy-*/ 포함)
              if [[ "$object" == __*/* ]] || [[ "$object" == build/* ]] || [[ "$object" == build-before* ]] || [[ "$object" == build-legacy* ]]; then
                printf "::notice:: ⏩ 유지: %s\n" "$object"
                KEPT_FILES=$((KEPT_FILES + 1))
              # NOTE: 루트의 버전 정보 파일은 삭제(폴더 안이 아닌 루트에 있는 파일만). __current_release- 로 시작하는 루트의 .txt 파일만 정확히 매칭
              elif [[ "$object" =~ ^${{ env.VERSION_FILE_PREFIX }}-.*\.txt$ ]]; then
                printf "::notice:: 🗑️ 이전 버전 파일 삭제: %s\n" "$object"
                TOTAL_DELETIONS=$((TOTAL_DELETIONS + 1))
                if ! aws s3 rm "${S3_BUCKET}/${object}" --region "$S3_REGION" 2>&1; then
                  printf "::warning:: ⚠️ 삭제 실패 (버전 파일): %s\n" "$object"
                  FAILED_DELETIONS=$((FAILED_DELETIONS + 1))
                fi
              else
                printf "::notice:: 🗑️ 삭제: %s\n" "$object"
                TOTAL_DELETIONS=$((TOTAL_DELETIONS + 1))
                if ! aws s3 rm "${S3_BUCKET}/${object}" --region "$S3_REGION" 2>&1; then
                  printf "::warning:: ⚠️ 삭제 실패: %s\n" "$object"
                  FAILED_DELETIONS=$((FAILED_DELETIONS + 1))
                fi
              fi
            fi
          done

          # NOTE: 삭제 결과 요약 출력
          printf "::notice:: ✅ S3 루트 정리 완료\n"
          printf "::notice:: 📊 정리 결과 요약:\n"
          printf "::notice::   - 유지된 파일: %d개\n" "$KEPT_FILES"
          printf "::notice::   - 삭제 시도: %d개\n" "$TOTAL_DELETIONS"
          printf "::notice::   - 삭제 실패: %d개\n" "$FAILED_DELETIONS"

          # NOTE: 삭제 실패가 있으면 경고 표시
          if [ "$FAILED_DELETIONS" -gt 0 ]; then
            printf "::warning:: ⚠️ 총 %d개 파일 삭제 실패\n" "$FAILED_DELETIONS"
            printf "::warning:: ⚠️ 권한 문제나 네트워크 오류가 발생했을 수 있습니다\n"
            
            # NOTE: 실패율이 50% 이상이면 배포 중단
            if [ "$TOTAL_DELETIONS" -gt 0 ]; then
              FAILURE_RATE=$((FAILED_DELETIONS * 100 / TOTAL_DELETIONS))
              if [ "$FAILURE_RATE" -ge 50 ]; then
                printf "::error:: 🚨 치명적 오류: 삭제 실패율이 %d%%입니다\n" "$FAILURE_RATE"
                printf "::error:: 🚨 S3 상태가 불안정하여 배포를 중단합니다\n"
                printf "::error:: 🚨 S3 권한 및 네트워크 상태를 확인하세요\n"
                exit 1
              else
                printf "::warning:: ⚠️ 삭제 실패율: %d%% (계속 진행)\n" "$FAILURE_RATE"
              fi
            fi
          fi

      - name: "🚀 새 버전 파일을 S3 루트로 복사"
        run: |
          S3_BUCKET="s3://${{ needs.validate-cd.outputs.S3_BUCKET_NAME }}"
          S3_REGION="${{ needs.validate-cd.outputs.S3_DEPLOY_BUCKET_REGION }}"
          BUILD_FOLDER="${{ env.BUILD_FOLDER }}"

          printf "::notice:: 🚀 %s 폴더를 S3 루트로 복사 중\n" "$BUILD_FOLDER"

          # NOTE: 이전 단계에서 루트를 이미 정리했으므로, 단순히 복사만 하면 됨
          if ! aws s3 cp --recursive --region "$S3_REGION" "${S3_BUCKET}/${BUILD_FOLDER}/" "$S3_BUCKET/"; then
            printf "::error:: 🚨 치명적 오류: 새 버전 파일 복사 실패\n"
            printf "::error:: 🚨 원본 경로: %s/%s/\n" "$S3_BUCKET" "$BUILD_FOLDER"
            printf "::error:: 🚨 대상 경로: %s/\n" "$S3_BUCKET"
            printf "::error:: 🚨 롤백을 시도합니다...\n"
            
            SNAPSHOT_FOLDER="${{ env.SNAPSHOT_FOLDER }}"
            printf "::notice:: 🔄 스냅샷에서 복구 시도: %s\n" "$SNAPSHOT_FOLDER"
            
            # NOTE: 롤백: 스냅샷에서 복구
            if ! aws s3 cp --recursive --region "$S3_REGION" "${S3_BUCKET}/${SNAPSHOT_FOLDER}/" "$S3_BUCKET/"; then
              printf "::error:: 🚨🚨🚨 심각한 오류: 롤백 실패 🚨🚨🚨\n"
              printf "::error:: 🚨 현재 S3 상태: 루트가 불완전한 상태입니다\n"
              printf "::error:: 🚨 스냅샷 경로: %s/%s/\n" "$S3_BUCKET" "$SNAPSHOT_FOLDER"
              printf "::error:: 🚨 즉시 다음 조치가 필요합니다:\n"
              printf "::error:: 🚨   1. S3 콘솔에서 %s 상태 확인\n" "$S3_BUCKET"
              printf "::error:: 🚨   2. 스냅샷 폴더가 존재하는 경우 수동으로 복구\n"
              printf "::error:: 🚨   3. 또는 이전 버저닝 폴더에서 재배포 시도\n"
              printf "::error:: 🚨 서비스 중단 가능성이 높습니다 - 긴급 대응 필요\n"
              exit 1
            fi
            
            printf "::notice:: ✅ 롤백 성공: 이전 버전으로 복구 완료\n"
            printf "::notice:: 📸 복구된 스냅샷: %s\n" "$SNAPSHOT_FOLDER"
            printf "::error:: 🚨 하지만 배포는 실패했습니다. 빌드 폴더를 확인하고 재시도하세요.\n"
            exit 1
          fi

          printf "::notice:: ✅ 파일 복사 완료: %s → S3 루트\n" "$BUILD_FOLDER"

      - name: "✅ 배포 완료 후 스냅샷 정리"
        run: |
          S3_BUCKET="s3://${{ needs.validate-cd.outputs.S3_BUCKET_NAME }}"
          S3_REGION="${{ needs.validate-cd.outputs.S3_DEPLOY_BUCKET_REGION }}"
          SNAPSHOT_FOLDER="${{ env.SNAPSHOT_FOLDER }}"

          printf "::notice:: ✅ 배포가 정상적으로 완료되어 스냅샷 삭제 중\n"

          # NOTE: 스냅샷 폴더 삭제
          aws s3 rm --recursive --region "$S3_REGION" "${S3_BUCKET}/${SNAPSHOT_FOLDER}/" || {
            printf "::warning:: ⚠️ 스냅샷 삭제 실패 (무시하고 진행)\n"
          }

          printf "::notice:: ✅ 스냅샷 삭제 완료\n"

      - name: "📄 S3 루트에 배포 버전 정보 파일 생성"
        run: |
          S3_BUCKET="s3://${{ needs.validate-cd.outputs.S3_BUCKET_NAME }}"
          S3_REGION="${{ needs.validate-cd.outputs.S3_DEPLOY_BUCKET_REGION }}"
          ENV_SUFFIX="$([[ "${{ inputs.DEPLOY_ENV }}" == "개발" ]] && echo "dev" || echo "prod")"
          VERSION="${{ inputs.VERSION }}"
          SERVICE_NAME="${{ inputs.SERVICE_NAME }}"
          DEPLOY_ENV="${{ inputs.DEPLOY_ENV }}"
          DEPLOY_USER="${{ github.actor }}"
          TIMESTAMP=$(date -u +"%Y-%m-%d_%H-%M-%S_UTC")

          # NOTE: 새로운 배포 버전 정보 파일 생성
          VERSION_FILE="${{ env.VERSION_FILE_PREFIX }}-${ENV_SUFFIX}-${{ inputs.SERVICE_NAME }}-${{ inputs.VERSION }}.txt"

          # NOTE: 파일 내용 생성 (헤더와 변수 정보를 한 번에 작성)
          cat > /tmp/version_info.txt << EOF
          ========================================
            배포 정보
          ========================================
          서비스명: ${SERVICE_NAME}
          배포 버전: ${VERSION}
          배포 환경: ${DEPLOY_ENV}
          배포 시각: ${TIMESTAMP}
          배포 실행자: ${DEPLOY_USER}
          ========================================
          EOF

          # NOTE: S3에 업로드
          aws s3 cp /tmp/version_info.txt "${S3_BUCKET}/${VERSION_FILE}" --region "$S3_REGION" || {
            printf "::error:: 🚨 버전 정보 파일 업로드 실패\n"
            exit 1
          }

          printf "::notice:: ✅ 배포 버전 정보 파일 생성 완료: %s\n" "$VERSION_FILE"

      - name: "🎉 배포 완료"
        run: |
          printf "::notice:: 🎉 배포가 성공적으로 완료되었습니다!\n"
          printf "::notice::   - 서비스: %s\n" "${{ inputs.SERVICE_NAME }}"
          printf "::notice::   - 버전: %s\n" "${{ inputs.VERSION }}"
          printf "::notice::   - 환경: %s\n" "${{ inputs.DEPLOY_ENV }}"
          printf "::notice::   - S3 버킷: %s\n" "${{ needs.validate-cd.outputs.S3_BUCKET_NAME }}"
          printf "::notice::   - 배포 시각: %s\n" "$(date -u +"%Y-%m-%d %H:%M:%S UTC")"
